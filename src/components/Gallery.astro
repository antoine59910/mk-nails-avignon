---
import siteData from "../data/site.json";
const { gallery } = siteData;
---

<section id="galerie" class="py-16 md:py-24 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12" data-reveal>
      <span class="text-primary font-semibold text-sm uppercase tracking-wider">Notre travail</span>
      <h2 class="text-3xl md:text-4xl font-display font-bold text-neutral-900 mt-2 mb-4">
        Découvrez nos créations
      </h2>
      <p class="text-lg text-neutral-700 max-w-2xl mx-auto">
        Quelques exemples de nail art et poses réalisées chez MK Nails
      </p>
    </div>

    <!-- Gallery grid -->
    <div class="grid grid-cols-2 md:grid-cols-3 gap-4 lg:gap-6" data-reveal-stagger>
      {gallery.map((item) => (
        <div class="group relative aspect-square rounded-xl overflow-hidden bg-neutral-100 hover:shadow-xl transition-all duration-300">
          <img
            src={item.image}
            alt={item.alt}
            class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            loading="lazy"
          />
          <!-- Caption overlay on hover -->
          <div class="absolute inset-0 bg-gradient-to-t from-neutral-900/80 via-neutral-900/0 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end">
            <p class="text-white text-sm font-medium p-4">
              {item.caption}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
